export function createSupportsColor(stream) {
  return {
    level: stream && stream.isTTY ? 1 : 0,
  };
}

const supportsColor = {
  stdout: createSupportsColor({ isTTY: true }),
  stderr: createSupportsColor({ isTTY: true }),
};

export default supportsColor;
