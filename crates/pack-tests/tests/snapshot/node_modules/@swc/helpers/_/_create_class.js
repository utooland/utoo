export function _(Ctor, protoProps) {
  for (const prop of protoProps || []) {
    Object.defineProperty(Ctor.prototype, prop.key, {
      value: prop.value,
      configurable: true,
      writable: true,
    });
  }
  return Ctor;
}
