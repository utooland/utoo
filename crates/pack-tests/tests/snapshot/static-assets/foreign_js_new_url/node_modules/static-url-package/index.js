export const resourceProxyUrl = new URL(
  './processes/resource-proxy/sw.js',
  import.meta.url,
).toString()

export function createMainWorker() {
  return new Worker(
    new URL('./processes/main/MainWorker.js', import.meta.url),
  )
}
