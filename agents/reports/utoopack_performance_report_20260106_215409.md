# 📊 Utoopack Performance Analysis Report

## 🛠 Build Context
- **Target**: `examples/with-antd`
- **Command**: `up build`
- **Trace Date**: 2026-01-06

---

## 🔍 Tier 1: The Runtime Backbone (P0) - [CRITICAL]
### 💥 A. Task Explosion (High Priority)
- **Finding**: Huge number of tasks: **1,941,783** calls to `turbo_tasks::function`.
- **Impact**: Total work sum spent in scheduler is ~30s, while actual work (parses/codegen) is < 8s. Over 75% of execution cost is scheduler overhead.
- **Hotspots**: 
  - `turbo_tasks::resolve_call`: 612,997 calls.
  - `task execution completed`: 293,131 calls.
- **Diagnosis**: The system is "over-tasked". Small operations that could be simple functions are likely wrapped in `#[turbo_tasks::function]`.

---

## 🔍 Tier 2: Physical & Resource Barriers (P1)
### 📁 C. I/O Chokepoints
- **Finding**: `read file` triggered 8,554 times (1,944 ms).
- **Diagnosis**: Reasonable for a project of this size, but many reads might be for `package.json` resolution probe fails.

### 🧵 D. Parallelization Slums
- **Finding**: Effective parallelism is high (~10x on a multi-core machine), but throughput is bottlenecked by the sheer volume of tasks rather than long serial chains.

---

## 🔍 Tier 4: The Asset Processing Pipeline (P3) (Transformation Logic)
### ⚛️ G. Pipeline Performance
- **G1. Parsing**: **29,166** `parse ecmascript` calls. This is disproportionately high for `with-antd` unless it's pulling in the entirety of Ant Design's source multiple times or splitting modules into many fragments.
- **G4. Chunking/Codegen**: `precompute code generation` (110,216 calls, 1.2s) indicates high complexity in mapping references to code.
- **G5. Finalization**: `minify ecmascript code` (2 calls, 943ms in heavy trace) is a significant point-bottleneck for the final stages.

---

## 💡 Optimization Strategy (Action Plan)

1. **Inline Simple Tasks**: Identify and remove `#[turbo_tasks::function]` from small getters or simple logic (like those in `library.rs`).
2. **Resolution Caching/Pruning**: Investigate the high frequency of `resolve_call`. Check if `ResolveOptions` are being needlessly varied, causing cache misses or re-resolution.
3. **Regex Tuning**: Ensure `ExternalsPlugin` regexes are compiled once and shared effectively.
4. **Batch I/O**: Consider if some `read file` calls can be consolidated.

---
*Report generated by Utoopack Performance Analysis Agent*
