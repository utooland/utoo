# 🔬 Utoopack Performance Analysis Report (Re-evaluating Baseline)

**Report ID**: `utoopack_performance_comparison_20260126_rebaseline`  
**Generated**: 2026-01-26 22:45:00  
**Baseline Trace**: `.trace/baseline_clean.json`  
**Optimized Trace**: `.trace/optimized_clean.json`  

---

## 📋 Executive Summary

This report re-evaluates the **Extension Priority Reordering** optimization following a dependency re-installation (clean `node_modules`). 

### 📊 Key Finding: Baseline Shift
The dependency re-installation significantly changed the baseline performance. The previous baseline (recorded 20260121) showed ~95k resolution calls. The **new clean baseline** already shows only **57k resolution calls**, representing a ~40% reduction from the historical state even without the extension optimization.

### 📊 Comparison Table (New Baseline vs. Optimized)

| Metric | Baseline (New Clean) | Optimized (Current) | Delta | Assessment |
|:---|:---:|:---:|:---|:---|
| **Resolving Calls** | 57,135 | **59,744** | **+2,609 (+4.5%)** | 🔴 Regressed |
| **Wall Clock Time** | 3,289.6 ms | **3,313.1 ms** | **+23.5 ms (+0.7%)** | ⚠️ Negligible |
| **Total Thread Work** | 57,883.5 ms | **57,849.9 ms** | **-33.6 ms (-0.05%)** | 🟢 Stable |
| **Parallelism** | 17.58x | 17.69x | +0.11x | 🟢 Improved |
| **Thread Utilization** | 50.3% | 48.5% | -1.8pp | ⚠️ Suboptimal |

---

## 🔍 Detailed Analysis

### 🗺️ Resolution Hotspots
Contrary to previous findings, reordering extensions to prioritize `.js` over `.ts` in `node_modules` resulted in a slight **increase** in the number of `resolving` calls (+2.6k). 

**Possible causes**:
1. **Dynamic Resolution Path**: In the current dependency structure, there may be more cases where a `.ts` file exists and is intended to be found, or where checking `.js` first triggers secondary resolution rules that were previously avoided.
2. **Statistical Noise**: With the baseline already significantly improved by the dependency refresh, the impact of extension ordering is now within the margin of error (~0.7% wall time diff).

### 💥 Critical Path Serialization
Both traces show that thread utilization remains the primary bottleneck (~50%). The build is limited by sequential task chains rather than pure filesystem probing overhead.

---

## 🚨 Conclusion & Recommendation

The previous optimization results (48% reduction) were likely inflated by a suboptimal dependency state in the 20260121 baseline. 

With a clean dependency structure:
1. The **baseline resolving calls** dropped from 95k to 57k naturally.
2. The **extension reordering** now shows a minor regression in call counts and negligible impact on wall time.

**Recommendation**: Revert the extension priority change as it does not provide a meaningful performance gain on a healthy dependency structure and may introduce subtle resolution order regressions. Focus instead on **[P0-A] Parallelizing Resolution Chains** as indicated by the 50% idle thread time.

---
*Generated by Utoopack Performance Analysis Agent*  
*Timestamp: 20260126_224500*
